namespace AdskLicensingModifier.Helpers;

public enum LicenseType
{
    Network,
    Standalone,
    User,
    Reset
}

public enum ServerType
{
    Single,
    Redundant,
    Distributed,
    Reset
}
